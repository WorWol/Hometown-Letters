# ═══════════════════════════════════════════════════════════════
# 故乡来信 — Docker Compose 开发环境
# ═══════════════════════════════════════════════════════════════
# 前置条件：安装 Docker Desktop
#   - Mac:     https://docs.docker.com/desktop/setup/install/mac-install/
#   - Windows: https://docs.docker.com/desktop/setup/install/windows-install/
#   - Linux:   https://docs.docker.com/desktop/setup/install/linux-install/
#
# 使用方式：
#   cp .env.example .env        # 首次：配置环境变量
#   vim .env                    # 填入你的 API Key
#   docker compose up           # 一键启动（热重载）
#   docker compose up -d        # 后台运行
#   docker compose down         # 停止
# ═══════════════════════════════════════════════════════════════

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hometown-letters
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      # 持久化数据（SQLite 数据库放在 backend/data/）
      - app-data:/app/backend/data
      # 持久化生成的图片
      - app-images:/app/backend/generated_images
      # 本地文本日志，按天轮转并保留 LOG_RETENTION_DAYS 天
      - app-logs:/app/backend/logs
      # 本地存储模式下，生成的 WebP 通过 /media 直接提供
      # 忽略容器内的 __pycache__，避免权限问题
      - /app/backend/__pycache__
    command: >
      uvicorn main:app
      --host 0.0.0.0
      --port 8787
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8787/api/admin/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  app-data:
  app-images:
  app-logs:
