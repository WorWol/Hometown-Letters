# Kimi K3:开放前沿智能

**Kimi K3 技术报告 · Kimi 团队**

> 本文件为 arXiv:2607.24653《Kimi K3: Open Frontier Intelligence》的逐句对照翻译,按章节分批进行。

---

## 摘要

我们推出 Kimi K3,一个拥有 2.8 万亿参数的混合专家(Mixture-of-Experts)模型,激活参数为 1040 亿,具备原生视觉能力和 100 万 token 的上下文窗口。Kimi K3 基于 Kimi Delta Attention(KDA)[63]和 Attention Residuals(注意力残差)[57]构建,二者改善了跨序列长度和模型深度的信息流。结合 Stable LatentMoE(每个 token 有效激活 896 个路由专家中的 16 个)以及改进的训练与数据配方,这些进展使整体扩展效率相比 Kimi K2 [58] 提升了约 2.5 倍。后训练的重点是在通用、智能体(agentic)和编码三个领域以及多个推理强度等级上开展强化学习,从而实现组合泛化和稳健的长程任务执行。在 2.8T 规模下,Kimi K3 得益于多方面的基础设施进展:KDA 的算法-系统协同设计、完美均衡的专家并行训练与高效内存管理、带持久 rollout 和沙箱状态的百万 token 智能体强化学习,以及部署方面的创新。

大量评估表明,Kimi K3 在长程编码、智能体、知识、推理和视觉任务上达到了前沿水平。尽管其整体性能仍落后于最强大的闭源模型(即 Claude Fable 5 和 GPT-5.6 Sol),但 Kimi K3 在我们的评估套件中持续优于其他开源和闭源模型。我们发布完整的 Kimi K3 模型权重,以促进未来研究并加速前沿智能的更广泛部署与应用。

---

## 1 引言

在大型语言模型(LLM)发展的大部分时间里,"扩展"(scaling)意味着在部署前投入更多算力,用更多数据训练更大的模型 [54, 45]。推理模型(reasoning model)的兴起确立了"测试时计算"(test-time computation)作为扩展的第二条轴线:OpenAI 的 o 系列扩展了强化学习和测试时推理 [84, 83];Anthropic 的扩展思考模型分配自适应的思考预算,并将推理与工具使用交错 [6, 7];DeepSeek-R1 [40] 和 Kimi K1.5 [118] 表明,大规模强化学习能够从强大的预训练模型中激发出复杂的推理行为;而 Kimi K2.5 Agent Swarm [59] 进一步将测试时扩展从串行推理延伸到并行智能体协作。这些进展使测试时扩展成为前沿研究的核心焦点。然而,尽管开源模型生态在第二条轴线上快速推进,在第一条轴线上却进展缓慢:许多近期模型仍停留在 1T 级参数规模之内或略高 [145, 29, 135, 120]。随着越来越精细的推理和智能体强化学习方法被应用于规模相近的预训练基座,开源的进步面临收敛停滞的风险,而与最强闭源系统的差距却在扩大。在 Kimi K3 中,我们同时沿两条轴线向前沿推进:将预训练基座扩展到前所未有的 3T 级参数,同时在 100 万上下文长度下扩展强化学习、推理强度和长程交互。

我们推出 Kimi K3,一个原生多模态混合专家模型,总参数 2.8 万亿,激活参数 1040 亿,上下文窗口达 100 万 token。其架构在序列长度、网络深度和模型宽度三个维度上扩展信息流。Kimi Delta Attention(KDA)[63] 提供高效的长序列混合,并周期性交错 Gated MLA 层以保留全局交互。Attention Residuals(AttnRes)[57] 允许每一层选择性地关注所有前序层的表示。Stable LatentMoE 将路由专家空间扩展到 896 个专家,每个 token 激活 16 个,同时归一化、SiTU-GLU 和 Quantile Balancing(分位均衡)在极端稀疏下稳定优化。这些架构进展结合改进的数据与训练配方,使整体扩展效率相比 Kimi K2 [58] 提升约 2.5 倍。

我们将这一预训练基座与专门为 100 万上下文测试时扩展设计的后训练相结合。Kimi K3 在长程编码、通用智能体、通用推理和知识任务上开展强化学习,每个领域都横跨多个推理强度等级。训练环境包括可验证搜索与专业知识工作、软件工程与内核优化、带视觉闭环工具使用的多模态推理、持久助手工作流、Web 开发以及自主执行任务。这些环境训练出一个"推理-行动-观察-验证-适应"的通用循环,通常历经数百乃至数千次工具调用、累计数百万上下文 token。领域和强度特化的策略通过多教师在策略蒸馏(multi-teacher on-policy distillation)[75, 134, 29] 被整合进一个统一模型。

实现这一规模需要能随架构复杂度、模型规模和轨迹长度而扩展的基础设施。针对 KDA 的系统协同设计,我们开发了融合内核、KDA 上下文并行和状态感知前缀缓存,使 KDA 在设备内、跨设备和跨请求都保持高效。针对 2.8T 参数 MoE 预训练,MoonEP 提供了完美均衡的专家执行,具备静态计算形状和零拷贝通信,同时高效内存训练和多模态编码器优化在有限内存内维持高利用率。针对百万 token 智能体强化学习,我们的混部(co-located)系统结合了部分 rollout、外部 KV 缓存保留、自适应限流和可恢复的 microVM 沙箱,以保存长生命周期的模型与环境状态。最后,专用内核以及缓存与预算感知的集群调度将这些创新转化为可预测的生产服务。

由此产生的模型确立了新的开源前沿。在涵盖长程编码、智能体、知识、推理和视觉任务的基准上,Kimi K3 的整体表现落后于最强的闭源系统(Claude Fable 5 和 GPT-5.6 Sol),但在我们的评估套件中始终领先于其他受测的开源与闭源模型,如图 1 所示。我们的贡献总结如下:

- **面向开源前沿的预训练。** 我们训练了一个 2.8T 参数的原生多模态 MoE 模型,激活参数 104B,上下文窗口 100 万 token。KDA、AttnRes、Stable LatentMoE 以及改进的数据与训练配方,共同将整体扩展效率相比 Kimi K2 提升约 2.5 倍。
- **面向多强度测试时扩展的强化学习。** 我们在通用、智能体和编码领域以及多个推理强度等级上开展 RL,然后将所得能力整合进一个统一模型。
- **面向万亿级参数、百万 token 智能的基础设施。** 我们引入了 KDA 系统协同设计;用于 2.8T 参数 MoE 预训练的 MoonEP 与高效内存基础设施;带可恢复沙箱、面向百万 token 智能体轨迹的混部 RL 系统;以及更多基础设施创新。
- **一个开放的前沿模型。** 我们发布完整的 Kimi K3 模型权重,使前沿智能可用于研究、部署和进一步创新。


---

## 2 模型架构

Kimi K3 的架构旨在沿三个互补维度扩展信息流:序列长度、网络深度和模型宽度。沿序列维度,混合注意力(Hybrid Attention)在每个 block 中将三层 Kimi Delta Attention(KDA)[63] 与一层 Gated MLA 结合,在为长上下文 token 混合提供高效机制的同时保留选择性高容量注意力(§2.1)。沿深度维度,Attention Residuals(AttnRes)[57] 使每个模块能够从嵌入、当前 block 和前序 block 中选择性检索表示,将信息获取延伸到常规顺序残差累加之外(§2.2)。沿宽度维度,每个注意力层后接一个 Stable LatentMoE 层执行稀疏通道混合,每个 token 有效激活 896 个路由专家中的 16 个(§2.3)。在原生视觉方面,MoonViT-V2 编码图像与视频,一个轻量投影器将所得视觉特征映射到共享嵌入空间,再送入主干网络处理(§2.4)。结合 Per-Head Muon(§2.5),这些组件提供了一个跨 token、跨层、跨通道扩展信息流的统一架构。配合改进的训练与数据配方,它们使整体扩展效率相比 Kimi K2 提升约 2.5 倍。图 2 给出了架构总览。

### 2.1 混合注意力

Kimi K3 采用线性注意力与全局注意力的逐层混合,将 KDA [63] 与 Gated MLA 结合。每个 block 包含 3 层 KDA 后接 1 层 Gated MLA,混合比为 3:1。该模式在整个主干网络中重复。两种注意力机制分别描述如下。主干网络末端额外放置一层 Gated MLA,确保最后一层始终执行全局注意力。

**2.1.1 Kimi Delta Attention**

KDA 用逐通道遗忘门 [63] 扩展 delta 规则递推 [105, 138]。考虑隐藏状态序列 xt ∈ R^d,其中 t 索引 token 位置,d 为模型隐藏维度。为清晰起见,我们先描述单个注意力头,其查询和键向量 qt, kt ∈ R^dk,值向量 vt ∈ R^dv,递推状态 St ∈ R^dk×dv。KDA 在 delta 规则更新前施加逐通道衰减:

St = (I − βt kt kt^T) Diag(αt) St−1 + βt kt vt^T,  ãot = St^T qt.   (1)

此处 αt ∈ (0,1)^dk 是逐通道单步保留因子,βt ∈ (0,1) 控制 delta 规则的写入强度。沿用 Kimi Linear [63],KDA 将逐头量参数化为(式 2,含 ShortConv、Swish、L2Norm 等投影)。查询、键、值投影先经 ShortConv 再经 Swish [138],查询和键进一步用 L2Norm [141] 归一化。低秩投影和逐头偏置 bh α ∈ R^dk 为每个键通道产生细粒度衰减 logit zh t。从 zh t 到 αh t 的下界有界映射在下面的分块形式之后引入。

**分块并行形式。** 沿用 Kimi Linear [63],KDA 跨块递推、块内并行。对块大小 C,X[t] 堆叠第 t 个块中的 token 向量(X ∈ {Q,K,V,O,U,W})。矩阵 S[t] ∈ R^dk×dv 表示进入块 t 的递推状态。对位置 1 ≤ i ≤ j ≤ C,定义逐通道累积衰减(式 3)。如同 Kimi Linear,Γ1->C [t] ∈ R^C×dk 逐行堆叠 γ1 [t], ..., γC [t]。UT 变换产生 U[t] 和 W[t],由此定义伪值项 eV[t] := U[t] − W[t]S[t]。给定输入状态 S[t],块 t 中所有输出并行计算(式 4):第一项携带来自前序块的信息,第二项计及当前块内的交互。UT 变换与分块形式的完整推导参见 Kimi Linear [63]。

**下界有界衰减。** 式 4 用倒数累积衰减 1/Γ1->C [t] 重缩放每个块的键。由于 Γ1->C [t] 是 (0,1) 中保留因子的乘积,该倒数可无界增长并在有限精度下溢出 [140, 63]。Kimi Linear 通过在对数空间计算相对衰减并将每块进一步切分为 16 token 的次级瓦片来控制数值范围 [140, 63]。非对角瓦片随后可直接在 Tensor Core 上用密集矩阵乘法计算。而对角瓦片仍需显式的位置对计算,这仍是块内的主要瓶颈。

Kimi K3 通过改变从衰减 logit zh t 到逐步对数衰减 gh t 的映射来解决该瓶颈。沿用 GDN 和 Mamba-2,Kimi Linear 使用负 Softplus 映射 gh t = −eAh Softplus(zh t) ∈ (−∞, 0)^dk [138, 24, 63]。Kimi K3 改用缩放 sigmoid 从下方界定对数衰减:

gh t = gmin Sigmoid(eAh zh t) ∈ (gmin, 0)^dk,  αh t = exp(gh t) ∈ (e^gmin, 1)^dk,   (5)

其中 Ah 是可学习的逐头对数尺度,gmin = −5 为固定值。我们初始化 Ah = 0,每个偏置 bh α 按 [63, 24, 138] 初始化。当 gmin = −5 时,每个保留因子满足 αh t,j > e^−5 ≈ 6.7×10^−3,16 token 瓦片上的累积对数衰减落在 (−80, 0) 内。对应的倒数重缩放因子因此小于 e^80,仍在 BF16 动态范围内。该有限范围使对角和非对角瓦片都能使用密集 Tensor Core 矩阵乘法,消除了位置对对角路径。此参数化与先前工作 [97, 27, 91] 中的下界有界递推门密切相关。图 3 展示了衰减参数化的变化及其计算后果。

**满秩门。** 最后,Kimi K3 将 KDA 的输出门从 Kimi Linear [63] 的低秩参数化改为依赖于输入的满秩投影。对递推输出施加逐头 RMSNorm [146] 后,KDA 施加数据依赖的输出门控 [99]:

yt = Wo[Sigmoid(Wg xt) ⊙ RMSNorm(ãot)].   (6)

**2.1.2 Gated MLA**

多头潜在注意力(MLA)由 DeepSeek-V2 [28] 引入,将每个 token 的键值表示压缩为低维潜在向量 ct = Wc xt。MLA 不缓存完整的逐头键和值,而是缓存 ct 并在注意力计算中通过学习的上投影重建内容键和值。该分解在保留全局 token 间注意力的同时减小了 KV 缓存占用。MLA 随后被 Kimi K2 和 Kimi K2.5 [58, 59] 采用,Kimi K3 在周期性全局注意力层中保留它。

与 Kimi K2 和 Kimi K2.5 不同,Kimi K3 遵循 Kimi Linear [63] 的混合设计,对所有 MLA 层应用无位置编码(NoPE)。因此,其查询或键不施加任何显式位置编码。中间的 KDA 层提供位置敏感且近因感知的序列混合,而 MLA 层提供不受限的全局内容交互。这种分离还避免了在扩展上下文长度时修改位置编码参数(如重新调优 RoPE 频率基或应用 YaRN [92])。

此外,Kimi K3 用一个依赖于输入的逐通道满秩输出门增强 MLA。设 ãot 为位置 t 处未加门的 MLA 输出,加门输出为:

yt = Wo[Sigmoid(Wg xt) ⊙ ãot].   (7)

门投影 Wg 为满秩,与 Kimi K3 中 KDA 的新参数化一致。该门允许每个 token 调制从全局注意力中读取的通道 [99]。

为校正 flash attention 中产生的有偏舍入误差,我们采用 [98] 的方法,在训练期间将注意力输出保持在 FP32。该选择使输出瓦片的片上占用翻倍;因此我们重新设计训练内核,将其与 KV 暂存缓冲区而非查询瓦片重叠,从而释放共享内存用于更深的 KV 流水线和更高的训练吞吐量。

### 2.2 Attention Residuals(注意力残差)

标准残差连接 [43] 将所有先前信息压缩到深度上的单一状态 hl 中——这一瓶颈类似于 RNN 在时间上的瓶颈。对于序列建模,Transformer 用注意力取代了递推 [10, 125],允许每个位置以数据相关的权重选择性访问所有先前位置。Attention Residuals(AttnRes)[57] 将同样的方法论应用于深度:每一层选择性地从所有前序层检索表示,而非均匀累加。

**完整注意力残差。** 对每一层 l,定义一个层特定的可学习伪查询 ql = wl ∈ R^d 以及键和值(式 8,其中 fi(hi) 为第 i 层输出,h1 为 token 嵌入)。注意力权重遵循 softmax 核 φ(q,k) = exp(q^T RMSNorm(k))[55, 146],其中 RMSNorm 防止输出幅度大的层主导权重(式 9)。由于网络深度适中(L < 100),该完整形式的 O(L²d) 运算量是可承受的;实际开销是保持所有层输出存活的 O(Ld) 内存(以及流水线并行下的跨阶段通信)。

**块注意力残差。** 为降低此开销,我们将 L 层划分为 N 个 block,每个 S = L/N 层。在 block n 内,层输出通过求和归约为单一表示 bn = Σ fj(hj),其中 b^i_n 表示该 block 前 i 层的部分和;我们令 b0 = h1,使 token 嵌入始终作为来源。跨 block 时,仅对 N 个 block 级表示施加完整注意力(式 10),键和注意力权重遵循式 8 和式 9。最终输出层聚合所有 N 个 block 表示。在块 AttnRes 下,内存和通信开销从 O(Ld) 降至 O(Nd),同时该块结构也界定了推理时状态,使并行的块间结果能通过在线 softmax [79] 与顺序的块内部分和更好地合并,显著降低推理时间开销。

经验上,N ≈ 8 即可在各模型规模上恢复大部分收益 [57];对 Kimi K3,我们将其层划分为 8 个 block、每个 12 层,计入嵌入层时给出一个部分末尾 block、共 9 个 block。

### 2.3 Stable LatentMoE

同时增大专家池和激活专家数会扩大专家特化空间,但在常规 MoE 中每个被选专家接收完整的 d 维 token 表示,因此通信和专家权重流量随路由倍数增长。LatentMoE [32] 通过将完整模型宽度与路由专家宽度分离使该扩展变得可承受:共享专家保留全宽路径用于通用变换,而特化的路由专家在宽度为 ℓ 的紧凑潜在空间中运作。这使 Kimi K3 能将通道混合扩展到 896 个路由专家、每 token 激活 16 个,对应稀疏度 56。

这种极端稀疏放大了原始设计的两种失效模式。第一,路由路径将 W↓、门控多分支专家前馈网络和 W↑ 组合成近四次连续矩阵乘法的链。这种病态结构结合 2.8 万亿参数规模,在路由分支产生爆炸性内部激活。第二,平衡近 10³ 个专家的负载超出了现有无辅助损失方法(式 11 为共享-路由专家组织,其中 u 为聚合路由表示,每层固定全宽共享专家数 Ns = 2)的适用范围。

Stable LatentMoE 用三个组件解决这两种失效模式:上投影前的 RMSNorm 与 Sigmoid Tanh Unit GLU(SiTU-GLU)以抑制激活爆炸,以及 Quantile Balancing(QB)用于负载均衡。

**2.3.1 归一化 LatentMoE**

原始 LatentMoE 直接对聚合路由表示 u 施加 W↑,其尺度随被选专家及其路由权重而变。如式 11 所示,Kimi K3 改在专家聚合与上投影之间插入 RMSNorm [146]。该归一化降低了路由分支在与全宽共享分支合并前对尺度变化的敏感性。除稳定训练外,额外的 RMSNorm 持续改善验证损失和下游基准。

**2.3.2 Sigmoid Tanh Unit GLU**

门控线性单元(GLU)用 sigmoid 激活的门调制线性值分支,计算 Sigmoid(Wg x) ⊙ Wu x [26]。SwiGLU 用 Swish(x) = x·Sigmoid(x) 替换 sigmoid 门,在 Transformer 中展现出强劲的实证表现 [107]。SwiGLU 随后成为大语言模型中广泛采用的 FFN 设计,而其有效性的完整解释仍是开放问题。

然而,SwiGLU 中两个乘性因子都无界,因此同时出现的大坐标可能产生激活异常值并增加低精度运算的溢出风险。原始 GLU 的 sigmoid 门避免了无界门增长,但未保留 Swish 近似线性的正区间。这促使我们设计一种既能控制大值增长、又能保留 SwiGLU 局部和正侧响应特性的激活。其他近期工作也探索了该权衡的替代参数化 [51]。

为满足这些要求,我们提出 Sigmoid Tanh Unit GLU(SiTU-GLU)。SiTU-GLU 将平滑封顶 softcap(x, β) = β tanh(x/β) 分别施加于 Swish 门的线性因子和上分支(式 12)。对 Kimi K3,我们将封顶超参数设为门分支 β1 = 4、上分支 β2 = 25。缩放 tanh 在原点附近近似线性、在大幅值处有界,使 SiTU-GLU 在保留 SwiGLU 局部响应的同时控制乘积中的两个因子。图 4 比较了 GLU、SwiGLU 和 SiTU-GLU 在公共切片上的分支定义与标量响应。§B 给出局部展开、极限情形、形式化输出界以及与硬截断的比较。

**2.3.3 Quantile Balancing(分位均衡)**

与基于辅助损失的路由 [33] 不同,Kimi K3 采用无辅助损失路由 [30]。负载均衡通过向用于 Top-k 选择的路由分数添加逐专家偏置 bj 来实现。对 token xi,路由器计算 si = Sigmoid(Wr xi) 并应用(式 13,其中 b 从 pi,j 中省略,故它调节调度而不改变混合权重或路由器的梯度优化)。原始方法以固定步长规则更新 b [30],其中 γ 在缓慢适应与负载振荡间权衡。随着 LatentMoE 将路由专家池增至每层 896,维持均衡负载变得更具挑战性。不均衡的路由会拖慢专家并行训练,并可能使部分专家训练不足 [47]。

为解决该限制,我们引入 Quantile Balancing(QB),它从与每个专家目标负载匹配的路由分数分位数设定专家偏置 [111]。考虑一批 m 个 token 路由到 n 个专家、Top-k 选择,目标负载为 q := mk/n 个 token/专家。QB 从单次前向传播导出下一个偏置。路由将 Top-k 选择替换为带偏置分数 si + b(t) 上的 Top-(k+1):前 k 项是实际路由,第 (k+1) 项是截止值 α(t)_i,即某专家要进入 token i 的 Top-k 必须超过的值。从 Top-(k+1) 路由取截止值避免了单独的 token 侧分位数。我们随后选择每个专家偏置使专家 j 收到其目标负载(单调性推导见正文),得 QB 更新(式 14):边际从原始分数 si,j 减去有偏截止 α(t)_i,故旧偏置仅通过截止值进入更新,第二行移除不影响 Top-k 选择的公共偏移。出于因果性,更新仅在下一步生效 [30]。图 5 展示了 m=8、n=4、k=1 的情形,每个专家收到目标负载 q=2。最终偏置在推理时冻结。均衡指派的推导见 §C。

**直方图估计。** 在规模上,式 14 中的分位数跨越整个全局 batch,其边际数以百万计且分散在各 rank 和累积步上,因此在训练时收集它们做精确分位数不可行。我们改为从各专家边际的直方图读取分位数:单次 all-reduce 汇总各 rank 的分箱计数,分位数从汇总计数中恢复。由于计数可加,无论 token 如何分片,直方图都表示汇总的全局 batch,因此估计反映整批分位数(精度受限于箱宽),通信成本仅为每专家数百个箱。该直方图估计器是我们在实践中使用的方法;§D 给出更详细描述及其误差界。

### 2.4 原生视觉

Kimi K3 是原生多模态的:文本、图像和视频在一个上下文中由单一共享主干处理,无需事后模态对齐阶段。该设计是 §1 所述长程、视觉闭环行为的架构基础。渲染输出及其生成代码存在于同一 token 流中,模型可编写代码、检视结果的截图或视频帧,并迭代改进视觉产物——用户界面、图形、视频——无需跨模型交接。

**MoonViT-V2。** 与 Kimi K2.5 的一个关键区别是,我们完全从零用下一 token 预测训练 Kimi K3 的视觉编码器 MoonViT-V2。以往做法(包括 Kimi K2.5 自身)从对比预训练模型(如 SigLIP)初始化视觉编码器,前提是预训练视觉知识给模型一个先发优势。我们背离该做法主要出于训练稳定性。当预训练编码器接入 LLM 时,联合优化变得不稳定:SigLIP 初始化的 MoonViT-3D 显示出持续更高的梯度范数和频繁尖峰,而 MoonViT-V2 在整个训练中保持稳定(图 6)。用下一 token 预测训练还使编码器表示直接由语言建模目标塑造,而非由偏向全局语义、忽视细粒度文本与结构线索的对比损失塑造。值得注意的是,我们发现 MoonViT-V2 在视觉评估中与 SigLIP 初始化的基线相当,表明对比预训练作为大规模多模态语言模型的初始化并非必要。

**架构。** 该训练配方建立在遵循 Kimi K2.5 [59, 61] 整体设计的视觉通路上:视觉输入先由 MoonViT-V2 编码,再由轻量 MLP 投影器映射进 LLM。MoonViT-V2 是一个 27 层视觉 Transformer,约 0.4B 参数,采用 RMSNorm 并从其线性和注意力投影中移除所有偏置项——该设计进一步稳定了上述从零优化。图像和视频以完全共享的参数处理,如同 MoonViT-3D:注意力被分解为帧内空间和帧间时间两次遍历,时间池化进一步沿时间维度压缩 token。投影前,2×2 下采样的 pixel-shuffle 操作将视觉 token 数减少四倍,使高达 3584×3584 像素的输入在 100 万 token 上下文内可承受。

### 2.5 Per-Head Muon

沿用 Kimi K2,Kimi K3 采用 Muon [53] 作为其矩阵参数的优化器。对注意力投影,我们进一步将其细化为逐头变体:不对完整 Q、K、V 投影矩阵施加 Newton–Schulz 正交化,而是沿头维度划分其动量矩阵,分别正交化每个头的块。直觉是:完整矩阵正交化将所有头视为单一耦合块,故梯度或动量尺度较大的头主导共享更新方向,而较小尺度的头获得归一化不足的更新;逐头正交化均衡了跨头的更新尺度。实践中,该设计产生更均衡的跨头学习动态,并改善更大规模下的训练稳定性。它还略微降低优化器开销,因为对高的逐头块做 Newton–Schulz 迭代比对完整投影矩阵更廉价。

